<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context=".MainActivity">

    <EditText
        android:id="@+id/ipfs_hash"
        android:hint="@string/ipfs_hash_input_hint"
        android:inputType="text"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/ipfs_hash_button"
        android:layout_below="@+id/ipfs_hash"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="@string/ipfs_hash_button"
        android:onClick="ipfsHashButtonOnClick" />
    <EditText
        android:id="@+id/ipns_hash"
        android:hint="@string/ipns_hash_input_hint"
        android:inputType="text"
        android:layout_below="@+id/ipfs_hash_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:layout_below="@id/ipns_hash"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/ipns_hash_button"
        android:onClick="ipnsHashButtonClick"/>
</RelativeLayout>
